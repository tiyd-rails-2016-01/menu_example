<%= form_for(@dish, url: dishes_path(@dish)) do |f| %>
  <% if @dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% @dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :price, {:style=>'color:#99AAEE'} %><br>
      <%= f.text_field :price %>
    </div>

    <div class="field">
      <%= f.label :name, {:style=>'color:#99AAEE'} %><br>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :description, {:style=>'color:#99AAEE'} %><br>
      <%= f.text_area :description %>
    </div>

    <div class="field">
      <%= f.label :course_id, {:style=>'color:#99AAEE'} %><br>
      <%= f.text_field :course_id, :readonly => true %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
